% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_gact.R
\name{hgtDB}
\alias{hgtDB}
\title{Hypergeometric Test on Gene Sets with Customizable Output}
\usage{
hgtDB(
  GAlist = NULL,
  sets = NULL,
  feature = NULL,
  featureIDs = NULL,
  minsets = 5,
  output = NULL
)
}
\arguments{
\item{GAlist}{A list containing gene annotation data. This parameter cannot be NULL.}

\item{sets}{A list or other structure containing sets of data for analysis.
This parameter cannot be NULL.}

\item{feature}{A character string specifying the type of feature to be used in the analysis.}

\item{featureIDs}{An optional vector of feature IDs to filter the analysis.}

\item{minsets}{A numeric value specifying the minimum number of sets to be considered.
Must be a positive integer. Defaults to 5.}

\item{output}{A character string specifying the format of the output.
Options include "p" for returning only p-values, "summary" for a summary
including both p-values and effect sizes, or NULL for the default full output.}
}
\value{
Depending on the \code{output} parameter, returns either a matrix of hypergeometric
test results, a matrix of p-values, or a list with p-values and effect sizes.
}
\description{
This function performs a hypergeometric test based on the provided parameters.
It retrieves feature sets from a database, constructs a design matrix, maps these sets,
and then applies a hypergeometric test. The output of the test can be customized
using the \code{output} argument.
}
\examples{
\dontrun{
  # Example usage for default full output
  hgt_results <- hgtDB(GAlist=myGAlist, sets=mySets, feature="myFeature")
  # Example usage for p-values only
  p_values <- hgtDB(GAlist=myGAlist, sets=mySets, feature="myFeature", output="p")
  # Example usage for summary output
  summary_results <- hgtDB(GAlist=myGAlist, sets=mySets, feature="myFeature", output="summary")
}

}

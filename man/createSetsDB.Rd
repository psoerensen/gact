% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_sets.R
\name{createSetsDB}
\alias{createSetsDB}
\title{Build all or selected GACT set modules}
\usage{
createSetsDB(
  GAlist,
  what = c("ensembl", "gene", "functional", "drug", "disease", "regulatory", "marker"),
  upstream = 35,
  downstream = 10,
  min_combined_score = 900,
  min_interactions = 5,
  overwrite = FALSE
)
}
\arguments{
\item{GAlist}{GACT database list (from \code{\link[=createDB]{createDB()}})}

\item{what}{Character vector of modules to build. Options:
\code{"ensembl"}, \code{"gene"}, \code{"functional"}, \code{"drug"}, \code{"disease"},
\code{"regulatory"}, \code{"marker"}. Defaults to all modules.}

\item{upstream}{Upstream distance (kb) for gene boundaries (default: 35)}

\item{downstream}{Downstream distance (kb) for gene boundaries (default: 10)}

\item{min_combined_score}{Minimum combined score for STRING/STITCH (default: 900)}

\item{min_interactions}{Minimum number of members per interaction set (default: 5)}

\item{overwrite}{Logical; if TRUE, rebuilds existing outputs}
}
\value{
Updated GAlist with all relevant mappings
}
\description{
Creates and updates Ensembl, gene–marker, functional, drug, disease,
regulatory, and marker-level mapping sets.

You can selectively build specific modules by providing \code{what}, e.g.:

\if{html}{\out{<div class="sourceCode">}}\preformatted{createSetsDB(GAlist, what = c("ensembl", "gene", "disease"))
}\if{html}{\out{</div>}}
}
